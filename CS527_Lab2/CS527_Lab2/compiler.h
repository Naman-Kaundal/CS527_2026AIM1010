#ifndef COMPILER_H
#define COMPILER_H

/* reads the source program from 'filename', compiles it, and writes
   the byte code to "program.byte" in the current directory. */
void compile(char *filename);

#endif
